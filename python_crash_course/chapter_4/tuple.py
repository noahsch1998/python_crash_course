# tuples are lists than can not be changed after creation
# create them by using parenthesis instead of brackets

dimensions = (200, 50)
for dimension in dimensions:
    print(dimension)

# to adjust the touple you muse redefine the entire touple
dimensions = (250, 75)
for dimension in dimensions:
    print(dimension)